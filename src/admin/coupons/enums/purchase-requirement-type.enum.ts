import { Status } from '@app/src/shared/enums'

export enum PurchaseRequirementType {
  NO_REQUIREMENT = Status.NO_REQUIREMENT,
  MINIMUM_PURCHASE = Status.MINIMUM_PURCHASE,
  MINIMUM_QUANTITY = Status.MINIMUM_QUANTITY,
}
