import { Query } from '@app/src/shared/enums'

export enum IncludesQuery {
  PARENT = Query.PARENT,
  CHILDREN = Query.CHILDREN,
  TRANSLATIONS = Query.TRANSLATIONS,
  TRANSLATIONS_LANGUAGE = Query.TRANSLATIONS_LANGUAGE,
}
